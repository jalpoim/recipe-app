{
  "enabledPlugins": {
    "figma@claude-plugins-official": true,
    "code-review@claude-plugins-official": true
  },
  "effortLevel": "medium",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; echo \"$f\" | grep -qE '\\.(js|jsx|ts|tsx)$' && pnpm exec prettier --write \"$f\" && pnpm exec eslint --fix --quiet \"$f\"; } 2>/dev/null || true",
            "statusMessage": "Formatting and linting..."
          }
        ]
      }
    ]
  }
}
